// designed the page as per the requirements

// Instruction
this is angular application. before starting the application
// run npm install - make sure all the dependency is installed
// move the project directory "cd my-app"
// run "ng serve" command
